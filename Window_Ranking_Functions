

---Ranking Functions---

---integrated ranking ----- DIscrete values
 Row_number() ---unique ranking,does not handle ties, no gaps in ranking

 Rank()over(partition by productid order by sales) shared rank, handles ties, gaps in ranking
    empty    optional               must
 
dense_rank() --- shared rank, handles ties, no gaps in ranking.
 Ntile()
      should have number 


--percentage based ranking--- continuous values
--cume_dist()
--percent_rank()
                         SQL WINDOW FUNCTIONS
                                │
                                ▼
                         RANKING FUNCTIONS
                                │
        ┌───────────────────────┼────────────────────────┐
        │                       │                        │
        ▼                       ▼                        ▼
   ROW_NUMBER()             RANK()                DENSE_RANK()
        │                       │                        │
        │                       │                        │
   Unique number            Same rank for           Same rank for
   for every row            same values            same values
        │                       │                        │
   No gaps                  Gaps occur              No gaps
   after duplicates        after duplicates        after duplicates
        │                       │                        │
        └───────────────────────┼────────────────────────┘
                                │
              ┌─────────────────┴─────────────────┐
              │                                   │
              ▼                                   ▼
        CUME_DIST()                         PERCENT_RANK()
              │                                   │
              ▼                                   ▼
     Cumulative distribution          Relative percentile
     of current row                   ranking of row
              │                                   │
              └─────────────────┬─────────────────┘
                                │
                                ▼
                           NTILE(n)
               NTILE(4) OVER (
                ORDER BY Sales DESC
                           )
                                │
                                ▼
                   Divides rows into n groups
                   of approximately equal size

ROW_NUMBER()
→ Unique number
→ No ties
→ Deduplication

RANK()
→ Same rank for ties
→ Gaps after ties

DENSE_RANK()
→ Same rank for ties
→ No gaps

CUME_DIST()
→ Cumulative distribution
→ 0 to 1
→ Proportion of rows at/below current value

PERCENT_RANK()
→ Relative rank
→ 0 to 1
→ (rank - 1)/(rows - 1)

NTILE(n)
→ Divide rows into n groups
→ Approximately equal groups


Row_number():

--ranking orders based on therir sales from highest to lowest

select orderid,productid,sales, row_number() over(order by sales)as sales_rank from sales.orders

---rank()---

select orderid,productid,sales, row_number()over(order by sales)as sales_rank,rank() over(order by sales)as sales_rankusing_rank from sales.orders


---dense_rank()---
select orderid,productid,sales, row_number()over(order by sales)as sales_rank,rank() over(order by sales)as sales_rankusing_rank, dense_rank() over(order by sales)as sales_rankusing_Dense_rank from sales.orders


Usecases

1 find the top highest sales from each product

select * from (select orderid,productid,sales, row_number() over( partition by productid order by sales)as sales_rank from sales.orders)t where sales_rank =1
 
 2 find the lowest 2 customers based on their total sales

 select orderid,customerid, sales from sales.orders

 select * from (select customerid,sum(sales) Total_sales, row_number() over(order by sum(sales))rk  * from sales.orders group by customerid )t  where rk <= 2;

 3 assign the unique id to the rows of the orders archieve target

 select * from sales.ordersArchive;

 select Row_number() over( order by orderid, orderdate) from sales.ordersarchive

 4 identify duplicate rows in the table 'orders archive and return a clean result without any duplicates

 select * from sales.ordersarchive;

 select * from (select Row_number() over(partition by orderid order by creationtime desc)rn, * from sales.ordersArchive)t where rn  > 1;



