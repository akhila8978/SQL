
aggregate functions: sum, avg,count, min, max  --- input is multiple values and output is single value

select count(*) as total_orders, sum(sales) as total_sales,avg(sales)as avg_sales,min(sales) as lowest_sales, max(sales) as high_sales   from sales.orders

---window functions---

perform calculations like aggregations on specific subset of data without losing the level of details of row

group by : return a single row for each group changes the granularity
 used for simple aggregations(count,sum,avg,min,max) 
 used for simple data analysis

window: returns a result for each row the granularity stays the same 
 used for aggragtions + keep details(count,sum,avg,min,max)
 rank functions: Row_number,rank,denserank,cum_dist,percent_rank,Ntile
 analytics functions:lead,lag,firstvalue,last value
  used for advanced data analysis

 
--- total sales across all orders---
select sum(sales) total_sales from sales.orders

-- total sales across all orders for each product--
select productid, sum(sales) total_sales from sales.orders group by productid 

---total sales across all orders for each product additionally provide details (orderid, orderdate)---

select orderid,orderdate, productid, sum(sales) total_sales from sales.orders group by productid, Orderid,OrderDate,productid

select orderid,orderdate, productid, sum(sales) over(partition by productid) totalsalesbyproducts from sales.orders

---Window function syntax---
window functions f(x) =over( partition + order by +frame)
avg(sales) over(partition by 

Argument type	Example	Meaning
Empty	RANK()	Function needs no argument
Column	AVG(Sales)	Pass a column
Number	NTILE(2)	Pass a number
Multiple arguments	LEAD(Sales,2,10)	Pass multiple values
Conditional logic	SUM(CASE WHEN Sales > 100 THEN 1 ELSE 0 END)	Use CASE inside the window function



  requiremenbt of parttition clause :

                    ┌─────────────────────┐
                    │   WINDOW FUNCTIONS  │
                    └──────────┬──────────┘
                               │
          ┌────────────────────┼────────────────────┐
          │                    │                    │
          ▼                    ▼                    ▼
 ┌─────────────────┐   ┌─────────────────┐   ┌─────────────────┐
 │ AGGREGATE       │   │ RANK            │   │ VALUE           │
 │ FUNCTIONS       │   │ FUNCTIONS       │   │ FUNCTIONS       │
 └────────┬────────┘   └────────┬────────┘   └────────┬────────┘
          │                     │                     │
          ▼                     ▼                     ▼
      COUNT(all data types)          ROW_NUMBER(Empty)          LEAD(expr,
      SUM(Numeric)            RANK(Empty)               offset, default)
      AVG(Numeric)             DENSE_RANK(Empty)         LAG(expr,         All data types
      MIN(Numeric)             CUME_DIST(Empty)          offset, default)
      MAX(Numeric)             PERCENT_RANK(Empty)       FIRST_VALUE(expr) last value()
                             NTILE(number)
          │                     │                     │
          ▼                     ▼                     ▼
     Expression            Empty / Number        Expression
     All Data Type
     Numeric

over --- will that we are using window function
----parition clause---
Partition divides the rows into groups , based on the columns

sum(sales) over() whole thing is one window
sum(sales) over(partitiom by product) --- divide according to product


                         ┌──────────────────────┐
                         │   WINDOW FUNCTIONS   │
                         └──────────┬───────────┘
                                    │
             ┌──────────────────────┼──────────────────────┐
             │                      │                      │
             ▼                      ▼                      ▼
     ┌───────────────┐       ┌───────────────┐      ┌─────────────────┐
     │   AGGREGATE   │       │     RANK      │      │     VALUE       │
     │   FUNCTIONS   │       │   FUNCTIONS   │      │   (Analytics)   │
     └───────┬───────┘       └───────┬───────┘      └────────┬────────┘
             │                       │                       │
             ▼                       ▼                       ▼
        COUNT(expr)             ROW_NUMBER()            LEAD(expr,
        SUM(expr)               RANK()                  offset,default)
        MAX(expr)               DENSE_RANK()            LAG(expr,
        MIN(expr)               CUME_DIST()             offset,default)
                                PERCENT_RANK()          FIRST_VALUE(expr)
                                NTILE(n)
             │                       │                       │
             ▼                       ▼                       ▼
        Expression:             Expression:             Expression:
        All Data Type           Empty / Numeric         All Data Type
        Numeric
             │                       │                       │
             └───────────────────────┼───────────────────────┘
                                     │
                                     ▼
                         ┌─────────────────────┐
                         │   PARTITION CLAUSE  │
                         │      Optional       │
                         └─────────────────────┘
                                     │
                                     ▼
                         ┌─────────────────────┐
                         │     ORDER CLAUSE    │
                         └──────────┬──────────┘
                                    │
                     ┌──────────────┴──────────────┐
                     │                             │
                     ▼                             ▼
              Aggregate                     Rank / Value
                Optional                      Required


select orderid,orderdate, sum(sales) over() Totalsales from sales.orders

select orderid, orderdate, sum(sales) over(partition by productid) totalsales from sales.orders

--- find the total sales for each combination of produc and order status additionally  provde details such as order id, order date

select orderid, orderdate, sum(sales) over(partition by productid,orderstatus)  as total_sales,
sum(sales) over(partition by productid,orderstatus) as salesbyproductorderstatus from sales.orders


